# Contributing to VOID//SIGNAL

The editorial principle is simple: raw signal, zero influence. Any contribution that introduces bias, suppresses categories, or games the bias scorer will be rejected.

## Commit Convention
```
feat(agent): add Reuters RSS ingestion
feat(scorer): improve bias detection for financial news
fix: resolve duplicate article deduplication
test: add bias scoring unit tests
```
